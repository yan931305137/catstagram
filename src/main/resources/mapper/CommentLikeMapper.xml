<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.catstagram.mapper.CommentLikeMapper">
	<!-- 点赞评论 -->
	<insert id="likeFeedComment" parameterType="com.catstagram.entity.CommentLike">
		INSERT INTO
			catstagram_comment_like (comment_idx, member_idx, created_at)
		values
			(#{comment_idx}, #{member_idx}, NOW())
	</insert>
	
	<!-- 取消点赞评论 -->
	<delete id="likeFeedCommentCancel" parameterType="com.catstagram.entity.CommentLike">
		delete from
			catstagram_comment_like
		where
			comment_idx = #{comment_idx} and member_idx = #{member_idx}
	</delete>
	
	<!-- 我是否点赞了这条评论 -->
	<select id="feedLikeCommentYN" parameterType="com.catstagram.entity.CommentLike">
		select comment_like_idx from catstagram_comment_like where comment_idx = #{comment_idx} and member_idx = #{member_idx}
	</select>
</mapper>